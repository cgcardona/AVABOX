const config: {
  networkId: number;
  fullNodeHost: string;
  fullNodePort: number;
  fullNodeProtocol: string;
} = {
  networkId: 12345,
  fullNodeHost: "localhost",
  fullNodePort: 9650,
  fullNodeProtocol: "http"
};

export default config;
